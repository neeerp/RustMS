version: '3.1'

services:
    db:
        image: postgres
        restart: always
        environment:
            POSTGRES_USER: maplestory
            POSTGRES_PASSWORD: pass
            POSTGRES_DB: maplestory
        ports:
            - 5432:5432
